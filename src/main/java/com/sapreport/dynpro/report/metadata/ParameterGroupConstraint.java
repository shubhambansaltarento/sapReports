package com.sapreport.dynpro.report.metadata;

/** How many of a {@link ParameterGroup}'s {@code members} must be selected. */
public enum ParameterGroupConstraint {
    EXACTLY_ONE,
    AT_MOST_ONE,
    AT_LEAST_ONE
}
